module.exports = async (req, res) => { const { paymentId } = req.query; if (!paymentId) { return res.status(400).json({ erro: 'Payment ID não fornecido' }); } try { const response = await axios.get(`https://api.mercadopago.com/v1/payments/${paymentId}`, { headers: { 'Authorization': `Bearer ${process.env.MERCADO_PAGO_TOKEN}`, 'Content-Type': 'application/json' } }); return res.status(200).json({ status: response.data.status, payment_id: response.data.id, amount: response.data.transaction_amount }); } catch (error) { console.error('Error checking payment:', error.message); return res.status(400).json({ erro: error.message }); } };